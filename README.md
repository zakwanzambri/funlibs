# PustakaPro

PustakaPro is a simple library management system built with Node.js and Express.
It allows you to add, edit, and delete books from a small SQLite database.

## Getting Started

Install dependencies:

```bash
npm install
```

Run the application:

```bash
node app.js
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
